"use client";
import { useState } from "react";
import { Sidebar } from "@/components/sidebar";
export default function Depositos(){
 const [items,setItems]=useState([{id:"main",name:"Depósito principal",code:"MAIN",status:"Ativo"}]); const [name,setName]=useState(""),[code,setCode]=useState("");
 function add(){if(!name.trim()||!code.trim())return;setItems(v=>[...v,{id:crypto.randomUUID(),name:name.trim(),code:code.trim().toUpperCase(),status:"Rascunho"}]);setName("");setCode("")}
 return <div className="shell"><Sidebar/><main className="main"><div className="topbar"><div><div className="eyebrow">ESTRUTURA LOGÍSTICA</div><h1 className="title">Depósitos</h1><p className="muted">Organize centros de estoque, corredores e posições.</p></div></div><div className="card section"><div className="form-grid"><label>Nome do depósito<input className="input" value={name} onChange={e=>setName(e.target.value)} placeholder="Ex.: Centro de distribuição"/></label><label>Código<input className="input" value={code} onChange={e=>setCode(e.target.value)} placeholder="Ex.: CD01"/></label></div><button className="btn" onClick={add}>Adicionar depósito</button><p className="muted" style={{fontSize:12}}>A persistência no Supabase será ativada quando a migration de depósitos for aplicada.</p></div><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Depósito</th><th>Código</th><th>Status</th></tr></thead><tbody>{items.map(i=><tr key={i.id}><td><b>{i.name}</b></td><td>{i.code}</td><td>{i.status}</td></tr>)}</tbody></table></div></section></main></div>;
}